import { Typography } from "@mui/material";
import React from "react";

export default function Logo() {
  return (
    <Typography
      variant="h1"
      style={{
        fontWeight: 400,
        fontSize: 40,
        color: "#9C9C9C",
        textAlign: "center",
        marginBottom: "1rem",
        marginTop: "1rem",
      }}
    >
      RGBdle
    </Typography>
  );
}
